name: slack-worktree-router
version: "0.4.0"
description: Route authorized Slack threads to persistent, isolated Codex sessions on a remote coding host.
api_version: 1
provides_hooks:
  - pre_gateway_dispatch
  - pre_tool_call
